import {defineMessages} from 'react-intl'

const PREFIX='bodygram.'

export default defineMessages({
  confirm:{
    id: PREFIX+'confirm',
    default:''
  },
  retake:{
    id: PREFIX+'retake',
    default:''
  },

})
